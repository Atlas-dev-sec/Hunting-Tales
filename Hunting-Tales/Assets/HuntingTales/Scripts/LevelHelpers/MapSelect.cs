using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class MapSelect : MonoBehaviour
{

    /*public Image mapLocked;
    public Image lockImg;

    private void Start() {
        mapLocked.enabled = true;
        lockImg.enabled = false;
    }
    private void Update() {
        
    }*/
   /* public int levelIndex;
    public GameObject pointsLevel;
    public GameObject go;



    [SerializeField] private Image mapLocked;
    [SerializeField] private Image lock;



    // Start is called before the first frame update
    void Start()
    {

        go  = transform.GetChild(0).gameObject;
         for(int i = 0; i < pointsLevel.Length ; i++) {
             go = pointsLevel.GetChild(i);
             Debug.Log(i);
         }
       Debug.Log(go.name);
    }

    /Update is called once per frame
    void Update()
    {
        
    }

    private void OnMouseDown() {
        Debug.Log(levelIndex);
    }*/
}
